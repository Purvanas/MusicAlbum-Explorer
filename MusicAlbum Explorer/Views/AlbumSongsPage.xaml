<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:MusicAlbum_Explorer.Models"
             x:Class="MusicAlbum_Explorer.Views.AlbumSongsPage"
             x:Name="page">

    <ContentPage.BindingContext>
        <models:Album />
    </ContentPage.BindingContext>

    <VerticalStackLayout Padding="20" Spacing="12">
        <Label Text="Chansons" FontSize="20" FontAttributes="Bold" />

        <CollectionView ItemsSource="{Binding Songs}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Button Text="{Binding Title}" CommandParameter="{Binding .}" Clicked="OnSongClicked" HorizontalOptions="FillAndExpand" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>

</ContentPage>